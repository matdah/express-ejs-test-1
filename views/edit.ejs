<!DOCTYPE html>
<html lang="sv">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Milligram CSS -->
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Roboto:300,300italic,700,700italic">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/normalize/8.0.1/normalize.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/milligram/1.4.1/milligram.css">
    <link rel="stylesheet" href="/css/styles.css">
    <script src="/js/main.js" defer></script>
    <title>Ändra | EJS-Test-1</title>
</head>

<body>
    <div class="container">
        <h1>EJS-Test-1</h1>
        <%- include('partials/main-menu'); %>
            <p>Det här är edit-sidan...</p>

            <h2>Ändra användaren <em>
                    <%= locals.user.name %>
                </em></h2>
            <form action="/edit/<%= locals.user.id %>" method="post">
                <% if (locals.error) { %>
                    <p class="error">
                        <%= locals.error %>
                    </p>
                    <% } %>

                        <label for="name">Namn</label>
                        <input type="text" name="name" id="name" value="<%= locals.user.name %>">
                        <label for="age">Ålder</label>
                        <input type="number" name="age" id="age" value="<%= locals.user.age %>">
                        <button type="submit">Spara ändringar</button>
            </form>
    </div>
</body>

</html>